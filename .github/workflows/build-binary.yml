name: cln app build binary

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build-and-release-linux:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2
      - uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.0.5'
          channel: 'stable'
      - name: Install dependencies
        run: sudo apt-get install -y clang cmake ninja-build pkg-config libgtk-3-dev liblzma-dev
      - name: Install project dependencies
        run: make dep
      #- name: Generate intermediates
      #  run: make gen
      - name: Enable linux build
        run: flutter config --enable-linux-desktop
      - name: Build artifacts
        run: flutter build linux --release
      - name: Archive Release
        uses: thedoctor0/zip-release@master
        with:
          type: 'zip'
          filename: clnapp-${{github.ref_name}}-linux.zip
          directory: build/linux
      - name: Upload Linux Binary
        uses: actions/upload-artifact@v2
        with:
          name: cln.app-linux
          path: build/linux/clnapp-${{github.ref_name}}-linux.zip
      - uses: montudor/action-zip@v1
        with:
          args: zip -qq -r clnapp-${{github.ref_name}}-linux.zip bundle
          working-directory: build/linux/x64/release/
      - name: Upload linux Binary
        uses: actions/upload-artifact@v2
        with:
          name: cln.app-linux
          files: build/linux/x64/release/clnapp-${{github.ref_name}}-linux.zip

  build-and-release-windows:
    runs-on: windows-latest

    steps:
      - uses: actions/checkout@v2
      - uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.0.5'
          channel: 'stable'
      - name: Install project dependencies
        run: flutter pub get
      - name: Generate intermediates
        run: flutter pub run build_runner build --delete-conflicting-outputs
      - name: Enable windows build
        run: flutter config --enable-windows-desktop
      - name: Build artifacts
        run: flutter build windows --release
      - uses: montudor/action-zip@v1
        with:
          args: zip -qq -r clnapp-${{github.ref_name}}-windows.zip bundle
          working-directory: build/windows/x64/release/
      - name: Upload windows Binary
        uses: actions/upload-artifact@v2
        with:
          name: cln.app-windows
          files: build/windows/x64/release/clnapp-${{github.ref_name}}-windows.zip

  build-and-release-macos:
    runs-on: macos-latest

    steps:
      - uses: actions/checkout@v2
      - uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.0.5'
          channel: 'stable'
      - name: Install project dependencies
        run: flutter pub get
      - name: Generate intermediates
        run: flutter pub run build_runner build --delete-conflicting-outputs
      - name: Enable macOS build
        run: flutter config --enable-macos-desktop
      - name: Build artifacts
        run: flutter build macos --release
      - uses: montudor/action-zip@v1
        with:
          args: zip -qq -r clnapp-${{github.ref_name}}-macos.zip bundle
          working-directory: build/macos/x64/release/
      - name: Upload Macos Binary
        uses: actions/upload-artifact@v2
        with:
          name: cln.app-macos
          files: build/macos/x64/release/clnapp-${{github.ref_name}}-macos.zip

  build-and-release-android:
    runs-on: ubuntu-latest
    steps:
        - uses: actions/checkout@v1
        - uses: actions/setup-java@v1
          with:
            java-version: '12.x'
        - uses: subosito/flutter-action@v2
          with:
            flutter-version: '3.0.5'
            channel: 'stable'
        - run: flutter pub get
        - run: flutter build apk
        - uses: actions/upload-artifact@v1
          with:
            name: cln.app-android-dev
            path: build/app/outputs/apk/release/app-release.apk